▶ 자바스크립트 객체
  *객체
   -사물의 속성과 동작을 묶어서 표현하는 기법
   
  *객체의 종류
   -내장객체: 생성자가 미리 작성되어있다.
   -사용자 정의 객체: 사용자가 생성자를 정의한다.
   
  *프로토타입
   -현재에는 메소드를 공유할 수 없다.
   -객체 안에 속성이나 메소드가 정의되어있으면 먼저 찾는다.
   -프토토타입 속성이나 메소드를 가지고 있는지 체크
   -원하는 속성/메소드를 찾을 때 까지 체인을 따라 올라간다.
   
  *Date객체
   -날짜와 시간작업을 하는데 사용되는 가장 기본적인 객체
   -setTimeout을 사용하여, 특정 함수를 1초마다 실행하도록 설정한다.
   
  *number객체
   -수치형 값을 감싸서 객체로 만들어주는 랩퍼 객체
   -var num = new Number(7);
   
  *String객체
   -length
   -prototype
   -constructer
   -split: 메소드는 문자열을 특정 문자로 잘라낸다는 의미이다.
   
  *Array객체
   -배열을 나타내는 객체
   -var myArray = new Array();
   -하나의 타입이 아닌, 다양한 타입의 내용이 들어갈 수 있다.
   -배열의 크기를 동적으로 조정할 수 있다.
   -concat은 두 개의 문자열을 붙일 때 사용한다.
   -indexOf는 몇 번째 원소인지 확인하기 위해 사용
   
  *오류 처리
   -자바스크립트에서의 예외 처리기는 try블록과 catch블록으로 이루어진다.
   -throw문장은 개발자가 오류를 생성할 수 있다.
